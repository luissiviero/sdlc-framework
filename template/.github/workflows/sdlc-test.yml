# Phase (d) test, fired by the build job's dispatch, or by the owner's sdlc:c-approved label (Full).
#
# Contract: docs/OPERATING_MODEL.md section 4.2 (the trigger and dispatch table), build guide
# step 30, article p.39-41 ("Run Claude Code non-interactively inside the CI/CD pipeline,
# sandbox the execution"). Installed into this project by /sdlc-init from the framework's
# template/.github/workflows/; edit it here, not in the framework.
#
# Token rule: events caused by this workflow's GITHUB_TOKEN never start another workflow run
# - only workflow_dispatch and repository_dispatch do. The owner's merges and labels fire
# these workflows normally; every automated transition is an explicit workflow_dispatch that
# run_phase.py sends with the change id. The job holds no deploy secret and no production
# credential (article p.40).
#
# /sdlc-init filled in the framework repository and the pinned Claude Code version.
name: SDLC test (d)

on:
  workflow_dispatch:
    inputs:
      change_id:
        description: "Four-digit change id, e.g. 0001"
        required: true
        type: string
  pull_request:
    types: [labeled]

permissions:
  contents: write
  pull-requests: write
  issues: write
  checks: write
  actions: write

concurrency:
  group: "sdlc-${{ inputs.change_id || github.event.pull_request.head.ref }}"
  cancel-in-progress: false

jobs:
  test:
    # Only the gate (c) approval label starts this workflow from a PR event; run_phase.py
    # also checks that a human, not the workflow token, applied it.
    if: ${{ github.event_name != 'pull_request' || github.event.label.name == 'sdlc:c-approved' }}
    runs-on: ubuntu-latest
    timeout-minutes: 150
    steps:
      - name: Check out the project
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Read the framework pin from sdlc.yaml
        id: pin
        run: python .github/scripts/sdlc_pin.py

      - name: Check out the pinned framework (public repository, no token)
        uses: actions/checkout@v4
        with:
          repository: "{{FRAMEWORK_REPO}}"
          ref: ${{ steps.pin.outputs.ref }}
          path: framework

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      # The pin step also prints sdlc.yaml's plugin.claude_code; re-install the workflows
      # when the two differ (/sdlc-init rendered the version below).
      - name: Install the pinned Claude Code CLI
        run: npm install -g @anthropic-ai/claude-code@{{CLAUDE_CODE_VERSION}}

      - name: Prepare the runner for Claude Code's sandbox
        run: python framework/plugin/ci/runner_setup.py

      - name: Run phase (d)
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          GITHUB_TOKEN: ${{ github.token }}
        run: python framework/plugin/ci/run_phase.py --root . --plugin-dir framework --phase d --id "${{ inputs.change_id }}" --head-ref "${{ github.event.pull_request.head.ref }}" --repo "${{ github.repository }}" --ref "${{ github.event.repository.default_branch }}"
