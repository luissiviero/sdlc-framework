Add a function double(x) to sample_pkg that returns x multiplied by two, export it from the
package like add and divide, and add a test for it under tests/. Run the tests and report done
only after they pass. End your answer with the test run's summary line as pytest printed it
(for example: 4 passed in 0.05s), not a paraphrase.
