# Handoff — session 6: the phase (f) shakedown (live checks 16–23 and the owed 8), the fixes they force, and the first eval cases from real runs

Session 5's brief is kept at `docs/handoffs/session-5-B5.md`; its result is on `main`: PR #44 (merged 2026-09-25, plugin 0.2.18 → 0.2.19, tag `v0.2.19`), the owner's pin and `CLAUDE.md` edits after it, and the sample repository's upgrade (`luissiviero/sdlc-sample-python` PR #23, merged 2026-09-25: the twelve workflows of 0.2.19, `lessons/README.md`, `evals/check.py`). Session 5 ran as one cloud session; its owner worked from a phone for the last part, so every edit of a guardrail file was given as "link; row; what is there; what it needs to be" (the format rule the owner wrote into the root `CLAUDE.md`, "Things Claude gets wrong") — keep that format.

What is true at the start of this session: every build stage is built (B0–B5; `docs/BUILD_GUIDE.md` carries a "Built" note on every step but the optional 33 and 40). Phase (f) — detection, diagnosis, routes, runbooks, gate (f), the dismissal store, the scan, the evals workflow, the hook log, the counters — is unit-tested (1096 tests) and **has never run live**: no detection run, no incident PR, no runbook, no dismissal, no scan has happened on a real repository. The sample's `SDLC evals` workflow is the one exception (it ran green on PR #23: an empty suite). Decisions 1–26 are all built; none is open.

## Preconditions — check before doing anything
1. `main` is at plugin 0.2.19 (`.claude-plugin/plugin.json`, `git ls-remote --tags origin | sort -V` ends with `v0.2.19`); this session starts from `main`. If not, stop and say so.
2. The root `sdlc.yaml` pins 0.2.19; the sample repository's `main` pins 0.2.19 and has `.github/workflows/sdlc-detect.yml`, `sdlc-runbook.yml`, `sdlc-scan.yml`, `sdlc-evals.yml` (PR #23).
3. `python tasks.py check` is green in this session before the first edit (1096 tests at the end of session 5) and `claude plugin validate .` is clean.
4. Sub-agent model: `CLAUDE_CODE_SUBAGENT_MODEL` was **unset** in session 5's cloud environment; every delegated task named `model: "opus"` explicitly and `/tasks` confirmed it. Do the same, or ask the owner to set the variable.
5. The sample repository must be in this session's GitHub scope with push access (`add_repo` with `access: push`, then the clone and `register_repo_root` as the tool says). Session 5 did this from the framework's session; the owner merges there too (`sdlc.yaml` is a guardrail file in the sample as well).

## Read in this order
1. `docs/OPERATING_MODEL.md` — the contract; §4 (gate (f)), §4.1 (the (f) row: what `/sdlc-maintain` may use), §4.2 (the trigger table: `sdlc-detect.yml`, `sdlc-runbook.yml`, `sdlc-scan.yml`, `sdlc-evals.yml`, the abandon row's dismissal step), §8 (the evidence files of (f): `detection.json`, `proposal.json`, `runbook-<name>.json`, `hook-log.jsonl`), §9 (`bands.yaml`, `maintain.runbooks`, `lessons/`).
2. `docs/DECISIONS.md` — 26 settled decisions. Do not reopen any. 14, 15, 25 and 26 are the ones the live checks exercise.
3. `docs/PROGRESS.md` — the session-5 record on top: the summary, choices 66–83 (how the loop was read where the pack left it open), the live checks 16–23 (**the work of this session**), the known gaps, the fresh-context review's sixteen findings, the "Between sessions 5 and 6" note.
4. `docs/NOTES.md` — platform facts; new in session 5: §13a–c (`claude plugin eval` rejected, the runs API's filters and paging as documented, `upload-artifact` v4). §11a's runs-API facts are what the live detection will confirm or refute.
5. `plugin/commands/sdlc-maintain.md`, `plugin/detect/cli.py` (`run | routes | dispatch | finish | go | dismiss`), `template/.github/workflows/sdlc-detect.yml` — the loop as built.
6. `docs/MODEL_ALLOCATION.md` — §2 for every session, §5a for this one.
7. `docs/reference/ai-native-sdlc-playbook.txt` p.42–47 (the loop), p.30 (the suite is built from real runs).
8. `CLAUDE.md` — conventions for this repo, including the format rule for owner edits.

## Objective of this session
Prove phase (f) live on the sample repository, fix what the live runs break, and record the results: the live checks 16–23 of `docs/PROGRESS.md` in their order (the rehearsed loop at tier 2 and 3, the three triage verbs, the Go, decision 26, the scan, the evals, the hook log, the counters) plus the owed check 8 (a close without a merge at gates (a)–(e)). Every framework defect a check reveals is fixed in this session's PR (plugin 0.2.20) with a regression test; every model-behaviour lesson becomes an eval case (article p.30: the suite is built from real runs, as session 5's two eval-authoring lessons already showed). No new build stage: the session ends with PROGRESS's live-check table filled in, and the loop either proven or its open defects named.

## What session 5 left ready (build on it, do not rebuild)
- **Detection** (`plugin/detect/`): `stats.py` (Western Electric over the last eight complete UTC days against the rolling baseline), `source.py` (the runs API through `urllib`, one observation per day, a file cache), `finding.py`, `dismissals.py` (`changes/.dismissed.json`; a detection dismissal expires after one window, a scan dismissal never), `routes.py` (decisions 14 and 26; a forced finding never pre-approves a runbook), `runbooks.py` (`quarantine-flaky-test`, `revert-pr`, a project `command`), `cli.py`.
- **The workflow** (`sdlc-detect.yml`): detect → (only when a change was filed) the model's `--diagnosis-only` session → the `finish` step (dispatch by the default branch's `bands.yaml`/`sdlc.yaml`, gate (f), evidence commit, the intent PR with `sdlc:f-ready` + `incident`); `workflow_dispatch(force_tier: "" | 2 | 3)` rehearses the loop and is recorded as `forced`.
- **Gate (f)** (`CHECKS_BY_PHASE["f"]`): artifacts (the intent with `## Evidence`), design_scope, guardrails, risk_list, detection, route. The incident change stays at phase `f` on `sdlc/<id>/a`; the owner's merge is gate (a) (`merged_at_gate_a`; the design guard accepts `f`).
- **Triage**: `schedule` label (nothing runs), `sdlc:go` label (`sdlc-runbook.yml` → `detect/cli.py go`, a person only), close with a comment → `sdlc-abandon.yml` → `detect/cli.py dismiss --open-pr` (the dismissal PR against the default branch, labelled `incident`).
- **Scan** (`plugin/scan/`, `sdlc-scan.yml`, weekly): the read-only review with the `security-baseline` skill, at most three incident intents per run, the scanners as artifacts.
- **Evals** (`plugin/evals/run.py`, `template/evals/check.py`, `sdlc-evals.yml`, this repository's `framework-evals.yml`): four cases in `evals/cases/`, 4 of 4 green on the third live run of session 5.
- **Observability**: every hook logs through `_common.run_hook`; the log is an artifact of every phase run; `pr/counters.py` is the last section of the digest.
- **Sample repository**: changes 0001 and 0002 merged; `main` pins 0.2.19 with all twelve workflows; the phase workflows have run live since session 3 (the API key and the OAuth token are in its secrets); the `.claude/settings.json` and `CLAUDE.md` are the 0.2.x ones.

## Deliverables (in dependency order)
1. **Live check 16, tier 2** — `workflow_dispatch` of `SDLC detect (f)` on the sample with `force_tier: 2` (this session may trigger it through the GitHub tools). Expected: a green job; the artifacts `sdlc-detect-log-<run id>` and `sdlc-hook-log-<run id>`; a change `000N-ci-test-failure-rate-…` on `sdlc/000N/a` with `evidence/detection.json` (`forced: true`), `intent.md` with `## Evidence`, `evidence/proposal.json` (`pull_request`), `gate-f.json` (`wait`); the PR `maintain(000N): …` with `sdlc:f-ready` + `incident`. Read the job log and the transcript (`evidence/claude-f.json`) whatever the outcome; every step that misbehaves is a defect to fix before the next check. Known unknowns the run will settle: the runs API's `created` filter and `Link` paging (NOTES §11a), the cache restore on a first run, `gh run view --log-failed` under the sandbox, the model's obedience to "read-only on source" and to `--diagnosis-only`.
2. **Live check 16, tier 3** — the same with `force_tier: 3` after the tier-2 incident is triaged (one finding per metric at a time; choice 69). The diagnosis may propose `runbook:quarantine-flaky-test`, `runbook:revert-pr` or `pull_request`; a forced runbook route resolves to `go` (choice 81), so the PR parks with "Go requested".
3. **Live check 17** — the three triage verbs on three incident PRs: merge (the design job starts; the guard reads `f` as `a`; the change reaches (b) — then close its design PR to keep the sample clean, which is also the owed check 8 at gate (b)), `schedule` (nothing runs; the digest lists it), close with a comment (the abandon job records `abandoned`, opens `dismiss(000N): …`; merge it; the next detection run says "dismissed until …").
4. **Live checks 18 and 19** — the Go and decision 26: a harmless project runbook (`maintain.runbooks: - name: rollback-deploy, command: python -c "print('rollback rehearsal')"`, `authorization: go`) added to the sample's `sdlc.yaml` and a `rollback-deploy` route in its `bands.yaml` — both owner-merged PRs (give the lines in the owner's format); `sdlc:go` applied by the owner (a label by this session's identity counts as a person too — record which identity applied it and whether the workflow accepted it); then `deploy.production: true` + `rehearsed_at` → the runbook runs at dispatch with the record naming decision 26.
5. **Live check 20** — `workflow_dispatch` of `SDLC scan`: the findings file and the scanner reports as artifacts, the `security:` intent PRs (the sample is tiny: zero findings is a valid result — then plant one harmless finding in a sample PR, e.g. a `subprocess` call with `shell=True`, and re-run), a dismissal through the close.
6. **Live checks 21 (the sample half is done: an empty suite is green), 22, 23** — read the hook-log artifact of a phase run and the committed `evidence/hook-log.jsonl`; the next daily digest's "## Counters" section (or `workflow_dispatch` of the digest).
7. **Fixes and evals** — every defect from 1–6 fixed in `plugin/` or `template/` with a regression test; the workflow copies under this repository's `.github/workflows/` kept identical to the template's (the template test asserts it); a new eval case per model-behaviour lesson; plugin 0.2.20 in `.claude-plugin/plugin.json` and `marketplace.json`. The sample's workflow copies are upgraded the way session 5 did it: `git rm .github/workflows/sdlc-*.yml`, re-run `plugin/init/sdlc_init.py --root <sample>`, one PR the owner merges.
8. **Carried small items** (only where a fix of 7 touches the same code): the `Diff.files` conversion; the per-decision panel cost (choice 52); an owner `accept` verb for a review finding (a new decision — ask, do not build); the release-page backfill for v0.2.15–v0.2.17 (optional); the deploy/status/rollback tools when an adapter names a real deployment (choice 35); a Node flaky-test runbook when a Node project needs one; session 3's still-owed Windows items (the watch setting, the test-file lock) — ask, do not assume. The optional root `CLAUDE.md` cosmetic: lines 24–26 lack the backticks the other Architecture lines have (owner's choice).
9. **Docs**: `docs/PROGRESS.md` rewritten for session 6 on top (same structure; the live-check table with results and run URLs; the session-5 record kept below as written); `docs/NOTES.md` §11a and §13 corrected from the live evidence (URL and quote); `README.md` if usage changed; OPERATING_MODEL only where the code changed; `docs/BUILD_GUIDE.md` and `docs/build_guide.json` kept identical if a step is touched; the next `HANDOFF.md` (if anything remains) with this one moved to `docs/handoffs/session-6-shakedown.md`.

## How the framework is tested
1. Self-tests (`python tasks.py check`): every hook, gate path and script with fixtures; commands, agents and skills statically; `claude plugin validate .` clean.
2. Fixture projects (`tests/fixtures/sample-python-project/`, `sample-node-project/`) for the runbooks, the dispatcher, the dismissal and the gate against a temporary copy with a bare remote; the end-to-end CI test with a fake `claude` (`tests/test_ci_end_to_end.py`); the eval runner and the scan with a fake `claude`.
3. Live checks on the sample repository — this session's main work: `workflow_dispatch` through the GitHub tools, the labels and the closes as the owner would do them (record which identity did what), the job logs and artifacts read from the run (the `curl` download of an artifact was denied by the repository's settings in session 5; use the GitHub tools' job logs and the committed evidence instead).
4. Evals (`Framework evals` on PRs to `plugin/**`, `template/**`, `evals/**`): one live run per push costs about $0.50 with the OAuth token; a red case is read from the job log (the runner records the answer's tail).

## Definition of done for this session
- `python tasks.py check` green (report the count) and `claude plugin validate .` clean.
- Live checks 16 (tier 2 and 3), 17, 18, 19, 20, 22, 23 and the owed 8 each have a result line in PROGRESS with the run URL and the PR number: proven, or the defect named and fixed (or named and left, with the reason).
- Every defect the checks revealed has a regression test; the template's workflow copies and this repository's are identical; plugin 0.2.20 (if anything in `plugin/` or `template/` changed).
- The lessons of the live runs are eval cases where they concern model behaviour, NOTES corrections where they concern the platform.
- `docs/PROGRESS.md` for session 6 with the ≤5-bullet summary on top and the list of what remains (if anything).

## Model rule for this session (`docs/MODEL_ALLOCATION.md` §1–2 and §5a, `docs/NOTES.md` §11c)
- The session model stays Fable: reading a live run's log, deciding whether a misbehaviour is the framework's or the model's, and choosing the fix are judgement. Opus, named `model: "opus"` explicitly (the environment variable was unset in session 5), writes the regression tests and the mechanical fixes from Fable's specs, and runs the fresh-context review of the diff before the commit. Do not set `CLAUDE_CODE_SUBAGENT_MODEL_FORCE`. Sub-agents that edit code get disjoint file sets.

## Rules for this session
- Never reopen a decision in `docs/DECISIONS.md`; if a live run shows one is technically impossible as built, document why in `docs/PROGRESS.md` and continue.
- Cite the PDF page in code comments or docs whenever the article is used.
- No notifications of any kind. No bypass-permissions mode. No third-party runtime dependency. Python for every script; the workflows are YAML whose `run:` lines call `python` (and the pinned `claude` install) only, never with a `${{ }}` in the line.
- `CLAUDE.md`, `.claude/**`, `REVIEW.md`, `sdlc.yaml` and `bands.yaml` are guardrail files here and in the sample: give the owner the exact edit as "link; row; what is there; what it needs to be", in `docs/PROGRESS.md` and in the reply, and leave the edit to the owner.
- A version bump goes into `.claude-plugin/plugin.json` and `marketplace.json` in the PR; the tag and the release page follow the merge by themselves. A cloud session cannot push a tag (NOTES §3b).
- Every PR: `python -m compileall -q plugin tests tasks.py .github/scripts`, the full `python -m pytest`, `python -m ruff check .`, `python -m ruff format --check .` — green before the push, **chained on the exit codes, never on the log's tail** (session 5 pushed one red commit that way; session 4 too), with the output in the PR body.
- The secrets-check hook refuses any file whose text contains a credential-shaped string, comments included: describe a key in words.
- Before the final commit run a fresh-context review of the diff against this handoff and the decisions; fix what it finds; then commit, push and open a draft PR against `main`.
- End with the ≤5-bullet summary at the top of `docs/PROGRESS.md`; the long version goes below it, and the session-5 record is kept below that as written.
