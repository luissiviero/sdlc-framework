# Handoff — session 4: build stage B4 (release gate, production-gate hook, deploy adapters; steps 29, 31, 32) and decisions 21–26 of 2026-09-23

Session 3's brief is kept at `docs/handoffs/session-3-B3.md`; its result is on `main` (PR #10, B3: `/sdlc-design`, the phase runbooks, the test-file lock, the review pass and `/sdlc-fix`, the PR summary and digest, the merge-triggered workflows), hardened by nine fix releases (plugin 0.2.1–0.2.9, PRs #12–#20) found by eleven live runs on `luissiviero/sdlc-sample-python`. `docs/PROGRESS.md` is the session-3 record this brief is written from, including the live-run results.

On 2026-09-22/23 (PRs #22, #23, documentation only) the plan was audited against the owner's original objective and the article by three independent fresh-context checks. The result: the build guide was amended to the platform facts and the build as it stands, decisions 21–26 were recorded and confirmed by the owner's merge, the Lite profile was withdrawn, build guide step 16a (deferred review) was added, and four steps were retagged ADAPTED. Nothing in the code changed that day; this brief is where those decisions become work.

## Preconditions — check before building anything
1. PRs #10 through #23 are merged to `main` and this session starts from `main`; if not, stop and say so. `CLAUDE.md` should say 47 steps and 26 settled choices; on 2026-09-23 the owner reported updating it, but `main` at 00eb437 still read "46 steps" and "20 settled choices", so the edit had not reached GitHub. If it still says 46 and 20, tell the owner once in the first reply and continue: the session cannot edit `CLAUDE.md` (guardrail file, rules below), and the old counts change nothing in this brief.
2. The framework is tagged at every version projects pin: tags `v0.2.0` through `v0.2.9` exist on `main` (the phase workflows check out `luissiviero/sdlc-framework` at `v<sdlc.yaml plugin.version>`; without the tag every phase job fails at checkout). Every version this session releases needs its tag on the merge commit — the owner pushes it; name the version in `docs/PROGRESS.md` so the owner knows which tag to create.
3. The owner's live checks from `docs/PROGRESS.md` ("Live checks the owner can do now" and "Results of the live checks so far") — read their status from the owner's first message; do not assume. Known at the end of session 3: the smoke run is green (step 3 closed); `/sdlc-init` upgraded the sample repository live; phase (b) has run headless end to end (seventh run) and passed its gate after two `/sdlc-fix` rounds; phase (c) reached its gate once (eleventh run, framework `v0.2.8`) and parked on `plan_sync` alone — the fix is 0.2.9, so the sample repository's `sdlc.yaml` pin must be bumped to 0.2.9 and `/sdlc:sdlc-fix 0001` run by the owner to re-run gate (c) on its draft PR #7; phase (d) has never run live; the repository setting "Allow GitHub Actions to create and approve pull requests" is on (NOTES §11a). Still owed from session 3's list: the Windows `python tasks.py check` run, the test-file lock denial on Windows, the agent-trigger check ("run the adversarial reviewer for phase c of change 0001"), and `CLAUDE_CODE_SUBAGENT_MODEL=opus` in the cloud environment. If any is unreported, ask once and continue, marking the dependent step partial; if the (c) gate still fails on 0.2.9, fixing that comes before any B4 step (the deploy workflow of 32.3 is one more transition on the same substrate).
4. `python tasks.py check` is green in this session before the first edit (486 tests at the end of the 2026-09-23 docs work; the count grows this session) and `claude plugin validate .` is clean. Note: in a container where `ruff` is a binary but not a Python module, `tests/test_ci_end_to_end.py::test_the_design_job_pushes_the_evidence_and_opens_the_pr` fails because the fixture's lint target is `python -m ruff check .`; `pip install ruff` fixes it, and it is not a framework fault.
5. Sub-agent model: `CLAUDE_CODE_SUBAGENT_MODEL=opus` should be set in the cloud environment's variables (owner action after session 3); either way, name `opus` in every sub-agent invocation and check `/tasks` after the first delegated task (Model rule below).

## Read in this order
1. `docs/OPERATING_MODEL.md` — the contract; §4.1 (phase composition) and §4.2 (workflow trigger table) were added in session 3 and are what B4 extends with the deploy transition; §10 (added 2026-09-23) lists, per decision 21–26, what changes in the model when it ships and where it lands.
2. `docs/DECISIONS.md` — 20 settled decisions plus 21–26 (confirmed on 2026-09-23 by the merge of PR #22). Do not reopen them; B4 executes decision 13 (gate (e) = merge, plus a release label only when `sdlc.yaml` declares a real production) and decision 19 (what deploy means for non-service projects), touches decision 7 (no third-party runtime dependency) in 31.1, and builds decisions 22, 24, 25, 23 and 21 in that order (deliverable 5). Decision 26 is B5.
3. `docs/PROGRESS.md` — what exists after session 3, the choices made (16–30), the live checks owed, the results of the eleven live runs, the known gaps carried to B4, and the B4 list.
4. `docs/NOTES.md` — platform facts; §11 (session 3: GitHub Actions facts with quotes, the Claude Code flags `run_phase.py` uses, the sandbox on runners, the sub-agent model facts). Re-verify a fact only if the minimum Claude Code version (2.1.278) changes.
5. `docs/BUILD_GUIDE.md` — steps 29, 31, 32 (B4), step 16a (deferred review, added 2026-09-23), the annotations of 2026-09-23 on steps 8, 9, 9a, 21, 23, 35, 37, 39, and, for the interfaces B4 must leave ready, steps 33–43 (B5). `docs/build_guide.json` is the identical machine-readable copy (47 steps). The owner's published copy of the guide, with a Built column per step, is https://claude.ai/artifact/1oRQDfgnubH6kC4ULPz539 (read-only for the session; the repo files are the source of truth).
6. `docs/reference/ai-native-sdlc-playbook.txt` — read a page when a step cites it (p.35–37 hooks as approval gates and the production-gate example; p.39–41 CI/CD steps 4–6, governance, DORA; p.32 the deploy tagline; p.45 closing the loop; p.42 the confidence gate and p.27 the verifier's fresh context, for step 16a; p.14–15 the human acceptance of the spec, which step 16a must keep).
7. `CLAUDE.md` — conventions for this repo.
8. `docs/MODEL_ALLOCATION.md` — §2 for every session, §4 for this one.

## Objective of this session
Complete build stage **B4**: the production-gate hook that blocks the guarded commands until the owner's release approval exists (step 29), the decision on deploy/status/rollback tooling (step 31), and the finished gate (e) — release notes, the release prepared per the project's deploy adapter, the deploy workflow that runs on the owner's merge (plus the release label when `deploy.production` is true) (step 32). Then build the decisions of 2026-09-23 in the order of deliverable 5: the fix trigger (22), the un-park labels (24), abandon (25), the GitHub-only check (23) and deferred review (21, step 16a), which also removes the Lite profile. Do not start the rest of B5 (worktrees, evals, detection loop, `/sdlc-maintain`, scans, telemetry, metrics, decision 26). Everything must still work by hand on the owner's PC and in a cloud session.

## What session 3 left ready (build on it, do not rebuild)
- **Phase runbooks**: `plugin/commands/sdlc-design.md`, `sdlc-build.md`, `sdlc-test.md`, `sdlc-deploy.md`, `sdlc-fix.md`; composition in OPERATING_MODEL §4.1. `/sdlc-deploy` already runs the review passes, the fix loop, gate (e) and marks the PR ready with `sdlc:e-ready`; its step 5 is where the release notes and the "release prepared" state of 32.1 go. `/sdlc-maintain` waits for B5 (step 37). Since 0.2.8 the build, test and deploy commands read `gate/preflight.py`'s `allow` (park check and `--phase`) instead of judging `status.yaml` themselves; since 0.2.9 `state/cli.py commit-phase` applies the plan-sync rule itself (plan.md required only in a commit that departs from the plan's file list), the hook covers raw `git commit`, and gate (c) re-checks the committed diff.
- **Deterministic tools** (all `python "${CLAUDE_PLUGIN_ROOT}/plugin/<pkg>/<script>.py"`): `state/cli.py` (`commit-phase` on the work branch — phases c, d, e share `sdlc/<id>/c` via `conventions.work_branch`; `set-phase`, `park`, `accept-risk`, `lock-tests`/`unlock-tests`); `state/status.py` (`read_status` is the one reader of `status.yaml` since 0.2.8: a stale `parked_reason` beside a non-parked gate result is dropped; `lift_stale_park` rewrites the file); `gate/cli.py` (`check`, `start-run`, `record-spend`, `set-iterations`, `bump-iteration`, `spec-header`); `gate/preflight.py`; `evidence/collect.py` (the three targets into `test.log`, `build.log`, `lint.log`, first line `# <command> — exit <code> — <seconds>s — <timestamp>`); `review/cli.py` (`prompt`, `validate` with finding signatures and the second-occurrence record `changes/.review-seen.json`, `check-run`); `pr/cli.py upsert` (the ≤5-bullet description from `plugin/pr/description.py`, labels, draft/ready; routes REST with the token first, then `gh`, then the compare URL — order changed in 0.2.3) and `pr/digest.py` (a pinned "SDLC review queue" issue whose body is rewritten daily); `ci/run_phase.py` (guard, labels, preflight, the composed `claude -p` with explicit `--allowedTools` per phase since 0.2.7, spend, dispatch, `ensure_pr`, `rerun_reason`, the run record and stderr committed on failure) with `ci/auth.py` (credential selection shared with the smoke script), `ci/project_setup.py` (`sdlc.yaml: commands.setup`, run by every phase job), `ci/runner_setup.py` (bubblewrap, socat, AppArmor) and `ci/settings.ci.json` (the CI sandbox and permission policy).
- **Gate checks added in session 3**: spec header (`Change id:`, `Produced by:`), `design_scope` at (b), `owner_actions` at (b)–(e) (risk acceptance and iteration resets are honoured only when their commit is not authored by the automation identity; `sdlc.yaml: automation_identity` overrides the default `github-actions[bot]`). Since 0.2.5 `risk_list` reads the diff paths and the spec's "Flagged concerns" section only. Known inconsistency found by the 2026-09-22 verification, carried as deliverable 6(d): `plan_sync` and `design_scope` judge the committed diff (merge base…HEAD) but `guardrails` and `risk_list` still read the merge base against the working tree plus untracked files (`gate/diff.collect`), while OPERATING_MODEL §3 says the gate judges HEAD rather than the working tree.
- **Hooks**: `plugin/hooks/test_file_lock.py` (fix-type changes, `status.yaml: tests_locked`, `sdlc.yaml: test_paths`) next to the four of step 15. The production-gate hook of step 29 is the next one: same pattern (`_common.py`, exec form in `hooks.json`, tests over stdin in `tests/test_hooks.py`), matcher `Bash|PowerShell`, parse the command like `plan_sync.py` does.
- **Workflows** in `template/.github/workflows/` (`sdlc-design.yml`, `sdlc-build.yml`, `sdlc-test.yml`, `sdlc-deploy.yml`, `sdlc-digest.yml`) and `template/.github/scripts/sdlc_pin.py`, installed create-only by `/sdlc-init`; `sdlc.yaml` gained `plugin.claude_code` (the CLI version the jobs install), `test_paths` and `commands.setup`. The deploy workflow of 32.3 is one more file in the same folder: trigger = the owner's merge of the build PR (`pull_request` `closed` + `merged == true`, head `sdlc/*/c`), plus the release label when `deploy.production` is true; it runs the adapter's action, never a phase run. The fix workflow of deliverable 5(a) is another file in the same folder. `tests/test_ci_end_to_end.py` runs `run_phase.py` as the workflows do against a fake `claude` and a fake `gh`; extend it for every new workflow.
- **Skills**: `spec-template` joined `intent-template` and `plan-template`; the five policy skills unchanged.
- **Agents**: the four of step 17, all `model: inherit`. Two observations from the live runs, carried as deliverable 6(e): the adversarial reviewer still has `Bash`, and it re-ran the gate's deterministic checks in the seventh run despite the 0.2.5 wording that forbids it.

## Deliverables (in dependency order)
1. **Release approval and the production-gate hook** (step 29; decision 13): 29.1 write into OPERATING_MODEL §4/§9 and the `sdlc.yaml` comments what counts as approval — the release label `sdlc:release-approved` on the build PR (applied by the owner; the hook reads it through the PR, never from a raw environment variable) or a signed tag on the merge commit — which command patterns the hook guards (from the project's `deploy.action` adapter, parameterised in `sdlc.yaml: deploy.guarded_commands`), that the hook only allows or blocks (never "ask": the run is unattended), and the block text with the route to approval. 29.2 `plugin/hooks/production_gate.py`, enabled by `deploy.production: true`, fail closed, exit 2 with the reason and the route; registered in `hooks.json`; tests over stdin. 29.3 every allow/block decision appended as one timestamped line to the file named by `SDLC_HOOK_LOG` (default `changes/.hook-log.jsonl`); the production-gate hook writes it first, the other hooks in B5 (41.1).
2. **Deployment tooling decision** (step 31): 31.1 decide and record in PROGRESS whether deploy/status/rollback MCP tools are built now — the plugin ships no runtime dependency (decision 7) and no project has a deploy target yet, so the likely answer is "document the interface in OPERATING_MODEL §9, build when a project's adapter names a real deployment"; 31.2 only if built. 31.3 DORA: record that the measures come from the CI system (p.41).
3. **Gate (e) finished** (step 32; decision 13): 32.1 `/sdlc-deploy` step 5 — release notes drafted, the release prepared per the adapter (`publish package` → the build artifact and the version bump proposed in the PR; `regenerate report` → the report regenerated into the PR; `deploy service` → the deploy manifest or changelog prepared, nothing deployed; `none` → nothing), the PR "waiting for your merge" with `sdlc:e-ready` (exists) and, when `deploy.production` is true, the sentence that the release label is the second act. 32.2 `plugin/release/notes.py`: release notes from the commits since the base, links to intent/spec/plan, the findings tally and the evidence status; deterministic and tested; `pr/description.py` quotes it at (e). 32.3 `template/.github/workflows/sdlc-release.yml`: on the owner's merge of the build PR (and the release label when production is declared) run the adapter's action from `sdlc.yaml: deploy` under the production-gate hook, with GitHub environment protection where the project uses it; installed by `/sdlc-init` like the others; parsed by the workflow test. 32.4 confirm (report only) that the post-deploy safety net will read step 37's bands and the rollback route.
4. **Carried from the live checks of session 3** (before or alongside step 29): (a) the web platform pushes `/sdlc-plan`'s intent to its assigned branch, so the design workflow's `sdlc/<id>/a` head rule never matches — make the merge-triggered workflows find the change from the merged `status.yaml` (the change whose phase is `a` on the default branch) instead of the head name, or have `commit-phase --push` also push the `sdlc/<id>/<phase>` ref; (b) `commit-phase` stages only changed paths; (c) bump `actions/checkout` and `actions/setup-node` to v5 in the template workflows and the smoke workflow (Node 20 deprecation annotation).
5. **Decisions 21–26 of 2026-09-23** (`docs/DECISIONS.md`; OPERATING_MODEL §10 has the one-line summary of each), in this order, each with its OPERATING_MODEL update in the same PR:
   - (a) **Decision 22 — the fix trigger.** `template/.github/workflows/sdlc-fix.yml` on `pull_request_review` `submitted` with state `changes_requested`, actor not the workflow token, the change found from `status.yaml` as item 4(a) settles and not from the head name (a web-session intent PR carries no `sdlc/<id>/a` head) → `run_phase.py --phase fix` (add `fix` to `RUNNABLE`, guard as the other phases, concurrency on the work branch) → `/sdlc-fix <id>`. Extend `/sdlc-fix` to gate (a): on an unmerged intent PR it applies the review comments to `intent.md` without re-running the brainstorm; this is the one exception to OPERATING_MODEL §4.1 ("nothing in a run edits intent.md") and §8 ("interactively") — write the exception into both sections. `sdlc-plan.md` already points the owner at `/sdlc-fix`; make that true. The command's `disable-model-invocation: true` stays (the trigger is the workflow, never the model). End-to-end test with the fake `claude` and `gh`: a "Request changes" review starts a fix round that pushes and refreshes the PR summary.
   - (b) **Decision 24 — un-park verbs as labels.** `sdlc:accept-risk`, `sdlc:reset-iterations`, `sdlc:unlock-tests`, applied by the owner on the PR; the next run reads them (`run_phase.py` guard, and the by-hand commands through `pr/github.py`), performs the corresponding state change, and records the label's actor in `status.yaml` (`risk_accepted_by`, `iterations_reset_by`, `tests_unlocked_by`); the `owner_actions` gate check accepts a label whose actor is not the automation identity, and keeps the commit-author rule for the CLI commands, which stay for by-hand runs. The pause flag stays in `sdlc.yaml`. `sdlc-design.yml`'s first-run label creation adds the three labels. Unit tests over recorded label payloads; the end-to-end test gains one case.
   - (c) **Decision 25 — abandon.** Closing a PR at gates (a)–(e) without merging sets `status.yaml: phase: abandoned` (a workflow on `pull_request` `closed` with `merged == false` for `sdlc/*` heads, or the next guard that sees the closed PR), branches kept, every workflow's guard skips an abandoned change, `state/cli.py show|list` display it; closing an incident intent PR both dismisses the finding (signature suppressed, step 39) and abandons its change. Gate (f) needs nothing: per-finding triage is the whole gate.
   - (d) **Decision 23 — GitHub only.** `/sdlc-init` refuses a project whose `origin` remote is not on github.com, with the reason (every trigger, identity, label, check run and the digest issue are GitHub-specific); OPERATING_MODEL §1 reads "any GitHub-hosted project". Unit test in `tests/test_init.py`.
   - (e) **Decision 21 — deferred review, build guide step 16a.** Only after (a), because the overturn route must exist first. Setting `sdlc.yaml: review: deferred` (per-change override in `status.yaml`), off by default. Inside a phase run, wherever the gate would park on an item of the fixed list — an open flagged concern in `spec.md`, contradicting policy skills, an `escalate` verdict of the adversarial reviewer (its routine / non-routine classification never parks; it only lowers the iteration cap), an Important review finding whose fix the run cannot determine, a verifier disagreement — the run calls the panel: (1) reviewer = the REVIEW.md pass prompt over the item and its context; (2) devil's advocate = the adversarial reviewer, blind to (1); both fresh contexts, the devil's advocate on a different model than the reviewer (`--model` on its `claude -p` run, or the agent's `model` frontmatter as a sub-agent; NOTES §11b, §11c); (3) conciliator = a fresh context that reads both verdicts and writes one decision with a rationale of at most five lines. The run applies the decision (closes the concern with the conciliator's text marked `decided-by: panel`, picks the policy reading, chooses the fix) and continues. Ledger: one line per decision in `changes/<id>/evidence/decisions-<phase>.md` (item, the two verdicts in one clause each, the decision, the rationale, the cost); `pr/description.py` opens the PR summary with "Decisions taken for you (N)" above the five bullets, and a review comment on any line overturns it through `/sdlc-fix`. Never to the panel, still a park: a risk-list hit, a guardrail file change, a run limit, an infrastructure failure, any merge to `main`, any deploy. Bounds: each panel call is three `claude -p` runs charged to the phase's budget (`gate.max_budget_usd`) and counted as one iteration; the panel may not raise a limit or edit a guardrail file. Gate checks (step 16) gain: "every panel decision has a ledger line" and "no panel decision touched a never-to-panel item". Lite removal in the same PR: from `state/conventions.py`, `state/cli.py`, the init, design, build and test commands, `run_phase.py` (`NEXT_WORKFLOW`, `_start_point`), `template/sdlc.yaml` and the build workflow; retire the Lite tests; OPERATING_MODEL §3 and §9 lose the Lite row and value. The Python halves (the ledger writer, the PR summary line, the gate checks, the `review: deferred` reading) are unit-tested; the panel's model-driven half is a prompt, exercised by the fake `claude` in the end-to-end test. If the session's budget runs out, (e) is the first item of B5, and the Lite removal moves with it.
   - Decision 26 (rehearsed rollback pre-approved on a declared production) is B5 with step 37; leave `maintain.runbooks[].rehearsed_at` documented in the `sdlc.yaml` comments only.
6. **Carried gaps from PROGRESS "Known gaps" and the 2026-09-22 verification** (small; do them where they touch code this session already changes): (a) `run_phase._start_point` starts `sdlc/<id>/c` from `sdlc/<id>/b` whenever that branch still exists, in every profile — restrict it to the Lite profile until 5(e) removes Lite, then to nothing; (b) the cloud environment's plugin cache is not refreshed by the setup script's `install` alone — add `marketplace update` + `plugin update` to the setup script documented in NOTES §3a; (c) monthly tuning of review findings (26.5) is not built — stays B5, note it in PROGRESS; (d) make `guardrails` and `risk_list` judge the committed diff (merge base…HEAD) like `plan_sync` and `design_scope`, so the gate matches OPERATING_MODEL §3, with `clean_tree` the only working-tree check; (e) the adversarial reviewer: remove `Bash` from its tools (the protected-path hook and the review pass remain the nets) and tighten its prompt so it never re-runs the deterministic checks; a regression test on the prompt text. The Full-profile approval-label check that degrades without `gh` or a token (PROGRESS known gaps) stays as is; the first Full-profile project is not yet in sight.
7. **Docs**: `docs/PROGRESS.md` rewritten for session 4 (same structure); `docs/NOTES.md` extended (cite URL and quote); `README.md` if usage changed; `docs/BUILD_GUIDE.md` and `docs/build_guide.json` kept identical if a step is touched (the check: every JSON `comments` field appears verbatim after "**Implementation notes.** " in the markdown); the next `HANDOFF.md` (B5) from the B4 progress, this brief archived under `docs/handoffs/session-4-B4.md`. The plugin version is bumped once per PR that changes plugin behaviour; PROGRESS names the tag the owner must push.

## How the framework is tested (unchanged; layer 3 stays in B5)
1. Self-tests (`python tasks.py check`): every hook, gate path and script with fixtures; commands, agents and skills statically; `claude plugin validate .` clean.
2. Fixture projects (`tests/fixtures/sample-python-project/`, `sample-node-project/`): the Python halves of every runbook against a temporary copy with a bare remote (`tests/test_integration_fixture.py` now covers (a) through (e)); the model-driven halves are prompts. `tests/test_ci_end_to_end.py` drives `run_phase.py` against a fake `claude` and a fake `gh`; every new workflow (release, fix) and every new guard rule (labels, abandon) gets a case there.
3. Evals and shakedown: B5; do not simulate. The live checks are the owner's: the (c) gate on the sample repository at 0.2.9, the first (d) run, the first merge-triggered release on a project whose adapter is `none` or `regenerate report`, and the first "Request changes" review that starts a fix round by itself.

## Definition of done for this session
- `python tasks.py check` green (report the count) and `claude plugin validate .` clean; the Windows run is a live check to ask for once if the code touched path handling.
- The production-gate hook denies a guarded command without approval and allows it with the release label or tag present (unit tests over stdin), and is inert when `deploy.production` is false.
- Every hook decision of the production gate lands in the log file with a timestamp and an allow/block verdict.
- The deployment-tooling choice is recorded in PROGRESS with its reason.
- `/sdlc-deploy` prepares the release per the adapter and the release-notes generator is tested; the release workflow is in the template, installed idempotently, and runs only on the owner's merge (plus the label when production is declared).
- A "Request changes" review starts a fix round in the end-to-end test, on an intent PR as well as on a design or build PR, and nothing in the run notifies anyone.
- The three un-park labels change state with the actor recorded, and the `owner_actions` check honours a label from the owner and rejects one from the automation identity.
- A closed, unmerged PR leaves the change abandoned and every workflow skips it.
- `/sdlc-init` refuses a non-GitHub origin with the reason.
- Deferred review, if reached this session: the panel runs only on the fixed list, never on a never-to-panel item; every decision has a ledger line and appears in the PR summary; the Lite profile is gone from code, template, commands and tests. If not reached, PROGRESS says so and the B5 handoff starts with it.
- OPERATING_MODEL sections 1, 3, 4.1, 5, 6, 8, 9 and 10 say what the code now does; §10 rows move into the body as they ship.
- `docs/PROGRESS.md` for session 4 with the ≤5-bullet summary on top and the list of what B5 needs.

## Model rule for this session (approved by the owner on 2026-09-21; facts verified against the Claude Code docs the same day, see `docs/MODEL_ALLOCATION.md` §1 and `docs/NOTES.md` §11c)
- The session model stays Fable. Delegated work runs on Opus: `CLAUDE_CODE_SUBAGENT_MODEL=opus` in the cloud environment's variables (environment selector above the message box → the environment's settings → "environment variables"; values are copied once at session start) or in the `env` block of `~/.claude/settings.json` on the PC; never in `template/`. Name `opus` in every sub-agent invocation anyway — the per-invocation model is first in the resolution order (v2.1.251+) and also covers the built-in Explore and Plan sub-agents, which ignore the variable on its own; only a forked sub-agent always inherits the session model.
- Check after the first delegated task with `/tasks` (it names the model on each sub-agent's row; v2.1.242+) — the authoritative check, only the owner can run it in the session UI. `/usage` is trusted on the PC only; the docs do not say what it shows in a cloud session. If a sub-agent row names Fable, stop delegating for quota reasons and say so in `docs/PROGRESS.md`.
- Delegate per `docs/MODEL_ALLOCATION.md` §2 and §4: give each sub-agent the step number, the article pages and the files to read, and ask for a short report, never file contents. Sub-agent output is input: Fable reviews every report and every draft before anything is written to a file.
- Fresh-context reviews run as general-purpose sub-agents on Opus, not through the plugin's `adversarial-reviewer` (its `model: inherit` would run on Fable).
- Do not set `CLAUDE_CODE_SUBAGENT_MODEL_FORCE`.
- Sub-agents that edit code must be given disjoint file sets (session 3 ran up to five in parallel; the only collisions were a shared test file and a version bump).

## Rules for this session
- Never reopen a decision in `docs/DECISIONS.md`; if one is technically impossible, document why in `docs/PROGRESS.md` and continue with the rest.
- Cite the PDF page in code comments or docs whenever the article is used.
- No notifications of any kind. No bypass-permissions mode. No third-party runtime dependency. Python for every script; the workflows are YAML that only call `python` and `claude`.
- The session does not touch `luissiviero/sdlc-sample-python` (out of the session's GitHub scope); the live proofs are the owner's.
- No live `claude -p` runs from inside the session; the CI job is the test bed for the model-driven halves.
- `CLAUDE.md`, `.claude/**`, `REVIEW.md` and `sdlc.yaml` are guardrail files: the session's permission settings deny editing `CLAUDE.md`, and the framework's own rule is that the owner changes guardrail files in a reviewed PR. Propose the exact lines in `docs/PROGRESS.md` and leave the edit to the owner (learned 2026-09-23).
- Documentation edits go through the Edit tool, sentence by sentence; the auto-mode classifier blocked a scripted rewrite of the docs on 2026-09-23 because its payload read like instructions (learned 2026-09-23).
- Before the final commit run a fresh-context review of the diff against this handoff and the decisions, fix what it finds, and only then commit, push and open a draft PR against `main`. The 2026-09-23 review of a docs-only diff found one blocking and fourteen smaller errors, so do not skip it for "small" changes.
- End with the ≤5-bullet summary at the top of `docs/PROGRESS.md`; the long version goes below it.
