# Handoff — session 7: finish the phase (f) shakedown (the owner-gated checks 17, 18 and 8), then the first real breach

Session 6's brief is kept at `docs/handoffs/session-6-shakedown.md`; its result is the session-6 record on top of `docs/PROGRESS.md` (the live-check table, the seven defects D1–D7, choices 84–88) and plugin **0.2.20**. Read that record first: it says which checks are proven and which were pending the owner's acts when the session closed. Keep the owner's format for every guardrail edit: "link; row; what is there; what it needs to be".

## Preconditions — check before doing anything
1. `main` is at plugin 0.2.21 (`.claude-plugin/plugin.json`; `git ls-remote --tags origin | sort -V` ends with `v0.2.21`). If not, the second session-6 PR (D8, D9) is not merged: stop and say so.
2. The sample repository's `main` pins 0.2.21 (`sdlc.yaml` line 73) and carries the 0.2.21 workflow copies (the `sdlc-fix.yml` reviewer rule; a sample upgrade PR made the way session 6 did it, `git rm` + `sdlc_init.py --root`) and the `CI` workflow (its PR #24 merged). If the pin is older, give the row and stop the live part until it is done.
3. `python tasks.py check` green (1128 tests at the end of session 6) and `claude plugin validate .` clean.
4. Sub-agents named `model: "opus"` explicitly (`CLAUDE_CODE_SUBAGENT_MODEL` was unset in sessions 5 and 6).
5. The sample repository in the session's GitHub scope with push access (`add_repo`, the clone, `register_repo_root`). The GitHub tools act as the owner's login (NOTES §14): a label, a comment or a close from the session is a person's act; leave merges to the owner.

## Read in this order
1. `docs/PROGRESS.md` — the session-6 record: the live-check table (what is pending), the defects, the choices, the guardrail lines, the known gaps.
2. `docs/NOTES.md` §14 — the platform facts the live runs settled (the runs API, the cache, the runner's git identity, `$GITHUB_OUTPUT`, `origin/HEAD`).
3. `docs/OPERATING_MODEL.md` §4 (gate (f)), §4.1 (the (f) row), §4.2 (`sdlc-detect.yml`, `sdlc-runbook.yml`, `sdlc-abandon.yml`), §9 (`maintain.runbooks`, `bands.yaml`).
4. `docs/DECISIONS.md` 14, 25, 26 (the ones the remaining checks exercise). Do not reopen any.
5. `plugin/detect/cli.py` (`open_incidents`, `_default_branch`, `cmd_go`, `cmd_dismiss`), `plugin/detect/routes.py apply_forced` (choice 81: a rehearsal never pre-approves a runbook).
6. `docs/MODEL_ALLOCATION.md` §2 and §5a.

## Objective
Close the live-check table: the rows session 6 left pending (17 merge, 17 `schedule`, the owed 8, 18 the Go) once the owner has merged the tier-3 incident PR #28 and added the `rollback-deploy` runbook rows; then, if the sample's `CI` workflow has eight complete days by then, read the first real (un-forced) detection run and record whether the statistics, `gh run view --log-failed` under the sandbox and the failed-run links behave. Every defect is fixed in this session's PR (plugin 0.2.21) with a regression test; every model lesson becomes an eval case; NOTES §14 grows with what the runs show.

## Deliverables (in dependency order)
1. **Check 17 merge + owed 8**: the owner merged PR #28 in session 6 and the design job failed on D8 (the model refused a change it read at `f`; fixed in 0.2.21). Re-run it with `workflow_dispatch(change_id: 0005)` of `SDLC design (b)` on the sample once it pins 0.2.21, confirm the run reaches gate (b) and opens the spec+plan PR, then close that PR without merging → the abandon job records `abandoned` on `sdlc/0005/b` (and `/a` if it still exists); the next `force_tier` run must file again (D6: `open_incidents` ignores a change abandoned on any of its branches — the live proof of that fix).
2. **Check 18 the Go**: with the owner's rows in `sdlc.yaml` (`maintain.runbooks: rollback-deploy`, `authorization: go`, `rehearsed_at`, `deploy.production: true`), `force_tier: 3` → if the diagnosis proposes `runbook:rollback-deploy` the PR parks with "Go requested" (a forced finding never pre-approves, even rehearsed: that is check 19's only live half); apply `sdlc:go` from the session (a person's act) → `sdlc-runbook.yml` runs the command once, records `runbook_authorized_by`, removes the label, the PR returns to `sdlc:f-ready`. If the diagnosis proposes `pull_request` instead (as it did on the empty rehearsal of session 6), say so and try once more with a comment on the PR asking for the runbook (a fix round, decision 22) or record it as a model lesson and write the eval case.
3. **Check 17 `schedule`**: on the last rehearsal PR, apply `schedule`; nothing runs; the digest lists it; then remove the label and close the PR without a comment so the metric is free again.
4. **The first real detection** (only if `CI` on the sample has eight complete UTC days of runs): read the scheduled run's log and artifact, `detection.json` (mean, sigma, the rule), and — if a breach was filed — the diagnosis's use of `gh run view --log-failed` (NOTES §14 says the sandbox's `gh` is unverified).
5. **Fixes and evals**: as in session 6 (plugin 0.2.21 if anything in `plugin/` or `template/` changes; the template's workflow copies and this repository's identical; the sample upgraded the way sessions 5 and 6 did it).
6. **Carried small items** (only where a fix touches the same code): the `Diff.files` conversion; the per-decision panel cost (choice 52); an owner `accept` verb for a review finding (ask, do not build); the release-page backfill for v0.2.15–v0.2.17 (optional); the deploy/status/rollback tools when an adapter names a real deployment (choice 35); a Node flaky-test runbook when a Node project needs one; session 3's Windows items (the watch setting, the test-file lock) — ask, do not assume. Four small ones from session 6: `/sdlc-init` could tell a project whose only workflows are the framework's that the metric will have no observation (choice 87); the dismissal reason recorded from a GitHub-tool comment carries the tool's footer (strip a trailing `--- _Generated by …_` line, or leave it); the fix round's comment collection (`/sdlc-fix` step 1 reads every unresolved review and comment whatever its author; filter by `author_association` or pass only the triggering review — the half of D9 the workflow gate does not close); and two on the Go path the session-6 review found (PROGRESS known gaps): `detect/cli.py go` reads `automation_identity` from the PR head's `sdlc.yaml`, and `sdlc-runbook.yml` runs the head's `commands.setup` before the Go step that holds the runbook secrets — read both from `origin/<default>` (the pattern of choice 80), with a test each.
7. **Docs**: `docs/PROGRESS.md` rewritten for session 7 on top (the session-6 record kept below as written); `docs/NOTES.md` §14 extended; `docs/OPERATING_MODEL.md` only where the code changed; the next `HANDOFF.md` with this one moved to `docs/handoffs/session-7-shakedown-2.md`.

## Rules for this session
- Never reopen a decision; a decision a live run shows impossible as built is documented in PROGRESS.
- Cite the PDF page whenever the article is used.
- No notifications, no bypass-permissions mode, no third-party runtime dependency, Python for every script, workflows whose `run:` lines call `python` (and the pinned `claude` install) only, never with a `${{ }}` in the line (an `env:` value may carry one).
- Guardrail files here and in the sample (`CLAUDE.md`, `.claude/**`, `REVIEW.md`, `sdlc.yaml`, `bands.yaml`): the owner's edit, given as "link; row; what is there; what it needs to be".
- One open incident per metric (choice 69): close (without a comment) or ship a rehearsal PR before the next `force_tier` run; never close a rehearsal PR with a comment (choice 86: every rehearsal shares one signature, and a dismissal would mute the next for a window).
- The shakedown method of session 6 (choice 88) when a fix must be live-tested before the tag: a sample branch whose `sdlc-detect.yml` checks out the framework at the branch under test, dispatched with that ref; the label- and close-triggered jobs still run the PR head's copies.
- Every PR: `python -m compileall -q plugin tests tasks.py .github/scripts`, the full `python -m pytest`, `python -m ruff check .`, `python -m ruff format --check .` — green before the push, chained on the exit codes; a fresh-context Opus review of the diff before the final commit; a draft PR against `main`.
- The secrets-check hook refuses any file whose text contains a credential-shaped string: describe a key in words.
