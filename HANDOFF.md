# Handoff — session 8: the first real breach on the sample, and what the owner decides next

Session 7's brief is kept at `docs/handoffs/session-7-shakedown-2.md`; its result is the session-7 record on top of `docs/PROGRESS.md` (the gaps of the shakedown closed in plugin **0.2.23**, PR #<<PR>>; the roadmap and the two proposals; choices 89–94) and `docs/NOTES.md` §15 (the environment facts read on 2026-09-25). Read that record first. Keep the owner's format for every guardrail edit: "link; row; what is there; what it needs to be".

## Preconditions — check before doing anything
1. `main` is at plugin 0.2.23 (`.claude-plugin/plugin.json`; `git ls-remote --tags origin | sort -V` ends with `v0.2.23`). If not, PR #<<PR>> is not merged or the tag workflow did not fire (NOTES §14: a `workflow_dispatch` of `sdlc-tag.yml` is the fallback): stop and say so.
2. The sample repository's `main` pins 0.2.23 (`sdlc.yaml` line 77) and carries the 0.2.23 runbook workflow (its upgrade PR #38, opened by session 7 from the 0.2.23 checkout, merged by the owner; the pin is the owner's edit after the tag). If the pin is older, give the row and stop the live part until it is in.
3. `python tasks.py check` green (1160 tests at the end of session 7) and `claude plugin validate .` clean.
4. Sub-agents named `model: "opus"` explicitly (`CLAUDE_CODE_SUBAGENT_MODEL` was unset in sessions 5–7).
5. The sample repository in the session's GitHub scope with push access (`add_repo`, the clone, `register_repo_root`). The GitHub tools act as the owner's login (NOTES §14): a label, a comment or a close from the session is a person's act; leave merges to the owner.
6. **The date.** The first real detection needs points, not days: `plugin/detect/stats.py` judges only with eight baseline points plus the eight-point tail (`MIN_BASELINE = 8`, `TAIL = 8`; PROGRESS choice 67), and a UTC day without a counted run of the sample's `CI` workflow gives no point. Count the days with runs since 2026-09-25 (`actions_list` on `ci.yml`, or the detect job's own log line) before planning the live part: below sixteen, the scheduled run logs "insufficient baseline" and the session does the rest of this brief.

## Read in this order
1. `docs/PROGRESS.md` — the session-7 record (what was fixed and how, the choices 89–94, the questions for the owner, the known gaps), then the session-6 record's live-check table (the run URLs are the reference for what a healthy run looks like).
2. `docs/NOTES.md` §14 and §15 — the platform facts of the live runs and the environment facts of session 7.
3. `docs/OPERATING_MODEL.md` §4.2 (`sdlc-detect.yml`, `sdlc-runbook.yml`, `sdlc-digest.yml` rows as 0.2.23 changed them) and §9.
4. `docs/ROADMAP.md` and the two proposals under `docs/proposals/` — read, not acted on, unless the owner has accepted one (its roadmap row then carries a change id).
5. `plugin/detect/stats.py` (the rules and the tiers), `plugin/detect/source.py` (what counts as an observation), `plugin/detect/cli.py` (`cmd_run`, `open_incidents`).
6. `docs/MODEL_ALLOCATION.md` §2 and §5b.

## Objective
Read the first real (un-forced) detection run of the sample and record whether the statistics, the diagnosis's use of `gh run view --log-failed` under the sandbox and the failed-run links behave; provoke one real 2σ once the baseline exists; close whatever the run shows, with a test per defect and an eval case per model lesson. Then the owner's decisions: the Milestone 1 bump, the order of R1 and R2, the two "ask" items.

## Deliverables (in dependency order)
1. **The first real detection** (only with sixteen points, precondition 6): read the scheduled 05:41 UTC run's log and the `sdlc-detect-log-<run id>` artifact, then `detection.json` on the filed branch if a change was filed (mean, sigma, `n_baseline`, `n_tail`, the rule, `forced: false`). If the baseline exists and nothing breached, provoke one bad day the article's way (p.45: one failure after a clean run): a sample PR whose test fails on purpose gives several failed `pull_request` runs on that day; close it without a merge and without a comment afterwards (choice 86 does not apply — a real finding has its own signature `detect|<metric>|<rule>` — but a close with a comment would dismiss the real finding for a window, which is not wanted). The next scheduled run should file at 2σ (`we2`) or 3σ (`we1`, likely with `sigma == 0` after a flat baseline: `min_sigma` in the sample's `bands.yaml` is the owner's floor, choice 68). Read the diagnosis transcript for the `gh run view --log-failed` calls: NOTES §14 says the sandbox's `gh` is unverified; record the outcome (allowed, denied, or never called) and, if denied, the fix (the bands' `tools` line or the sandbox's allowed domains).
2. **Gaps from the run** (and any fix deliverable 3 produces), each with a test, and an eval case per model lesson, in this session's PR (plugin 0.2.24 if anything in `plugin/` or `template/` changes; the template's copies and this repository's identical; the sample upgraded the way sessions 5–7 did it: `git rm .github/workflows/sdlc-*.yml`, `plugin/init/sdlc_init.py --root <sample>`, `git restore -- sdlc.yaml`, one PR).
3. **The `--bare` check** (PROGRESS session 7, "Known gaps"): if any phase run of the sample or of another project has run with `ANTHROPIC_API_KEY` since session 7, read its `evidence/claude-<phase>.json` for the project's `CLAUDE.md` content in the system prompt; absent → file the fix (pass it explicitly in `run_phase.py`; NOTES §10/§13 say bare mode skips it); present → close the gap. No such run → carry it.
4. **The owner's answers**, if given by then (PROGRESS session 7, "Questions for the owner"): the Milestone 1 bump (a `plugin.json` edit the session makes in the PR; the tag follows the merge); the `accept` verb (a new decision: draft the DECISIONS entry, do not build before it is accepted); the Windows items (the watch setting, the test-file lock: build only what the owner confirmed); R1 or R2 accepted → the roadmap row gets the change id and the work goes through `changes/<id>-<slug>/intent.md` with `Entry route: idea`, `Framework change: yes`, in a separate change, not in this session's fix PR.
5. **The three items the session-7 review left** (PROGRESS session 7, "Known gaps"; each with a test): the head's own `.github/scripts/sdlc_pin.py` and its short `--ref origin/<default>` in the twelve workflow copies (fully qualify the ref; read the script from the default branch); the runbook job's park on a failed setup step; the deterministic collection of the fix round's reviews and comments by `author_association` in `run_phase.py --phase fix`. Then the **carried small items** (only where a fix touches the same code): the `Diff.files` conversion; the per-decision panel cost (choice 52); the release-page backfill for v0.2.15–v0.2.17 (optional); the deploy/status/rollback tools when an adapter names a real deployment (choice 35); a Node flaky-test runbook when a Node project needs one; the phase workflows' setup step reading the head's `commands.setup` (choice 90 says why it is not a hole today; revisit only if a fork PR ever reaches a phase job).
6. **Docs**: `docs/PROGRESS.md` rewritten for session 8 on top (the session-7 record kept below as written); `docs/NOTES.md` §16 with the run's facts (read date on every fact, choice 93); `docs/OPERATING_MODEL.md` only where the code changed; `docs/ROADMAP.md` rows updated if a candidate moved; the next `HANDOFF.md` with this one moved to `docs/handoffs/session-8-first-breach.md`.

## What the owner decides next (from session 7)
- The number of the Milestone 1 bump (`docs/ROADMAP.md`, "How it is marked"), once the last leftover of its table is done.
- R1 (`docs/proposals/okf-adoption.md`) and R2 (`docs/proposals/session-tooling-pdf-web.md`): accept, order, or leave proposed. R2's edit tables (a Python SessionStart hook, the permission entries, the "Tooling rules" section of `CLAUDE.md`) are the owner's to apply; nothing in them was committed.
- The two "ask, do not build" items carried since session 5: the `accept` verb for a review finding; session 3's Windows items.
- The three guardrail rows of PROGRESS session 7 (the root pin, the sample's pin, the `CLAUDE.md` line-3 mention of the roadmap).

## Rules for this session
- Never reopen a decision; a decision a live run shows impossible as built is documented in PROGRESS.
- Cite the PDF page whenever the article is used.
- No notifications, no bypass-permissions mode, no third-party runtime dependency, Python for every script, workflows whose `run:` lines call `python` (and the pinned `claude` install) only, never with a `${{ }}` in the line (an `env:` value may carry one).
- Guardrail files here and in the sample (`CLAUDE.md`, `.claude/**`, `REVIEW.md`, `sdlc.yaml`, `bands.yaml`): the owner's edit, given as "link; row; what is there; what it needs to be".
- One open incident per metric (choice 69): a real finding blocks the metric until its PR is triaged; triage it (merge, `schedule` or close) before expecting the next filing, and remember that a close with a comment dismisses its signature for one window.
- The shakedown method of session 6 (choice 88) when a fix must be live-tested before the tag: a sample branch whose `sdlc-detect.yml` checks out the framework at the branch under test, dispatched with that ref.
- Every PR: `python -m compileall -q plugin tests tasks.py .github/scripts`, the full `python -m pytest`, `python -m ruff check .`, `python -m ruff format --check .` — green before the push, chained on the exit codes; a fresh-context Opus review of the diff before the final commit; a draft PR against `main`.
- The secrets-check hook refuses any file whose text contains a credential-shaped string: describe a key in words.
- An environment fact (what the container can reach or run) is recorded in NOTES with its read date and re-tested only when the date is older than the environment's last change (choice 93).
