# Handoff — session 7: finish the phase (f) shakedown (the owner-gated checks 17, 18 and 8), then the first real breach

Session 6's brief is kept at `docs/handoffs/session-6-shakedown.md`; its result is the session-6 record on top of `docs/PROGRESS.md` (the live-check table, every row proven; the ten defects D1–D10; choices 84–88) and plugins **0.2.20, 0.2.21 and 0.2.22** (PRs #47, #48, #49, all merged and tagged on 2026-09-25; the sample pins 0.2.22). Read that record first. Keep the owner's format for every guardrail edit: "link; row; what is there; what it needs to be".

## Preconditions — check before doing anything
1. `main` is at plugin 0.2.22 (`.claude-plugin/plugin.json`; `git ls-remote --tags origin | sort -V` ends with `v0.2.22`). If not, the third session-6 PR (D10) is not merged: stop and say so.
2. The sample repository's `main` pins 0.2.22 (`sdlc.yaml` line 77 after the owner's runbook rows) and carries the 0.2.22 workflow copies (the abandon and runbook workflows' own concurrency groups; the sample's upgrade PR made the way session 6 did it) and the `CI` workflow (its PR #24 merged). If the pin is older, give the row and stop the live part until it is done.
3. `python tasks.py check` green (1128 tests at the end of session 6) and `claude plugin validate .` clean.
4. Sub-agents named `model: "opus"` explicitly (`CLAUDE_CODE_SUBAGENT_MODEL` was unset in sessions 5 and 6).
5. The sample repository in the session's GitHub scope with push access (`add_repo`, the clone, `register_repo_root`). The GitHub tools act as the owner's login (NOTES §14): a label, a comment or a close from the session is a person's act; leave merges to the owner.

## Read in this order
1. `docs/PROGRESS.md` — the session-6 record: the live-check table (every row proven, with run URLs and PR numbers), the defects D1–D10, the choices 84–88, the known gaps (D11 and the rest of deliverable 3).
2. `docs/NOTES.md` §14 — the platform facts the live runs settled (the runs API, the cache, the runner's git identity, `$GITHUB_OUTPUT`, `origin/HEAD`).
3. `docs/OPERATING_MODEL.md` §4 (gate (f)), §4.1 (the (f) row), §4.2 (`sdlc-detect.yml`, `sdlc-runbook.yml`, `sdlc-abandon.yml`), §9 (`maintain.runbooks`, `bands.yaml`).
4. `docs/DECISIONS.md` 14, 25, 26 (the ones the remaining checks exercise). Do not reopen any.
5. `plugin/detect/cli.py` (`open_incidents`, `_default_branch`, `cmd_go`, `cmd_dismiss`), `plugin/detect/routes.py apply_forced` (choice 81: a rehearsal never pre-approves a runbook).
6. `docs/MODEL_ALLOCATION.md` §2 and §5a.

## Objective
Every live check of session 6's table is proven (16 both tiers, 17 all three verbs, the owed 8, 18, 19 as far as a rehearsal can prove it, 20 with its dismissal, 21, 22, 23). What remains is the loop on real data and the small gaps the live runs left: read the first real (un-forced) detection run once the sample's `CI` workflow has eight complete days and record whether the statistics, `gh run view --log-failed` under the sandbox and the failed-run links behave; close the gaps listed under deliverable 3. Every defect is fixed in this session's PR (plugin 0.2.23) with a regression test; every model lesson becomes an eval case; NOTES §14 grows with what the runs show.

## Deliverables (in dependency order)
1. **The sample at 0.2.22**: confirm precondition 2; if the pin or the copies are older, give the owner the rows and the upgrade PR (the way session 6 did it: `git rm .github/workflows/sdlc-*.yml`, `plugin/init/sdlc_init.py --root <sample>`, `git restore -- sdlc.yaml`, one PR) and stop the live part until they are in. Then close the digest's D11 gap first if you plan a rehearsal (below), because the rehearsal sequence of session 6 still applies (one open incident per metric; close a rehearsal PR without a comment).
2. **The first real detection** (only if `CI` on the sample has eight complete UTC days of runs — it started on 2026-09-25; a run on `main` per merge and on every PR): read the scheduled 05:41 UTC run's log and artifact, `detection.json` (mean, sigma, `n_baseline`, the rule), and — if a breach was filed — the diagnosis's use of `gh run view --log-failed` (NOTES §14 says the sandbox's `gh` is unverified). To provoke a real 2σ once the baseline exists, a PR whose test fails on purpose gives one bad day (the article's own example, p.45); close it without a merge afterwards.
3. **Gaps from the live runs**, each with a test: **D11** the digest lists a scheduled incident without its `schedule` label (`pr/digest.py`: show the owner's labels beside the gate label); the half of D9 the workflow gate does not close (`/sdlc-fix` step 1 collects every unresolved review and comment whatever its author — filter by `author_association` or pass only the triggering review); the two Go-path items the session-6 review found (`detect/cli.py go` reads `automation_identity` from the PR head's `sdlc.yaml`; `sdlc-runbook.yml` runs the head's `commands.setup` before the Go step that holds the runbook secrets — read both from `origin/<default>`, the pattern of choice 80); `/sdlc-init` telling a project whose only workflows are the framework's that the metric will have no observation (choice 87).
4. **Fixes and evals**: as in session 6 (plugin 0.2.23 if anything in `plugin/` or `template/` changes; the template's workflow copies and this repository's identical; the sample upgraded the way sessions 5 and 6 did it).
5. **Carried small items** (only where a fix touches the same code): the `Diff.files` conversion; the per-decision panel cost (choice 52); an owner `accept` verb for a review finding (ask, do not build); the release-page backfill for v0.2.15–v0.2.17 (optional); the deploy/status/rollback tools when an adapter names a real deployment (choice 35); a Node flaky-test runbook when a Node project needs one; session 3's Windows items (the watch setting, the test-file lock) — ask, do not assume; the dismissal reason recorded from a GitHub-tool comment carries the tool's footer (strip a trailing `--- _Generated by …_` line, or leave it).
6. **Docs**: `docs/PROGRESS.md` rewritten for session 7 on top (the session-6 record kept below as written); `docs/NOTES.md` §14 extended; `docs/OPERATING_MODEL.md` only where the code changed; the next `HANDOFF.md` with this one moved to `docs/handoffs/session-7-shakedown-2.md`.

## Rules for this session
- Never reopen a decision; a decision a live run shows impossible as built is documented in PROGRESS.
- Cite the PDF page whenever the article is used.
- No notifications, no bypass-permissions mode, no third-party runtime dependency, Python for every script, workflows whose `run:` lines call `python` (and the pinned `claude` install) only, never with a `${{ }}` in the line (an `env:` value may carry one).
- Guardrail files here and in the sample (`CLAUDE.md`, `.claude/**`, `REVIEW.md`, `sdlc.yaml`, `bands.yaml`): the owner's edit, given as "link; row; what is there; what it needs to be".
- One open incident per metric (choice 69): close (without a comment) or ship a rehearsal PR before the next `force_tier` run; never close a rehearsal PR with a comment (choice 86: every rehearsal shares one signature, and a dismissal would mute the next for a window).
- The shakedown method of session 6 (choice 88) when a fix must be live-tested before the tag: a sample branch whose `sdlc-detect.yml` checks out the framework at the branch under test, dispatched with that ref; the label- and close-triggered jobs still run the PR head's copies.
- Every PR: `python -m compileall -q plugin tests tasks.py .github/scripts`, the full `python -m pytest`, `python -m ruff check .`, `python -m ruff format --check .` — green before the push, chained on the exit codes; a fresh-context Opus review of the diff before the final commit; a draft PR against `main`.
- The secrets-check hook refuses any file whose text contains a credential-shaped string: describe a key in words.
