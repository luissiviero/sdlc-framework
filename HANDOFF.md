# Handoff — session 9: the first real breach on the sample, from a session that can reach it

Session 8's brief is kept at `docs/handoffs/session-8-first-breach.md`; its result is the session-8 record on top of `docs/PROGRESS.md` (the session-7 review's three leftovers and the origin check closed in plugin **0.2.24**; choices 95–98; the scope fact) and `docs/NOTES.md` §16 (the environment facts read on 2026-09-26). Read that record first. Keep the owner's format for every guardrail edit: "link; row; what is there; what it needs to be".

## Preconditions — check before doing anything
1. **The session's GitHub scope holds both repositories.** Session 8 was started on this repository alone: every GitHub tool call on `luissiviero/sdlc-sample-python` was refused ("not configured for this session") and `add_repo` was denied by the permission classifier (NOTES §16). The scope is chosen when the session starts, so the owner starts session 9 with the sample selected; the first act of the session is one read of the sample (`actions_list` on `ci.yml`) to prove it. Without it, stop the live part and say so.
2. `main` is at plugin 0.2.24 (`.claude-plugin/plugin.json`; `git ls-remote --tags origin | sort -V` ends with `v0.2.24`). If not, the session-8 PR is not merged or the tag workflow did not fire (NOTES §14: a `workflow_dispatch` of `sdlc-tag.yml` is the fallback): stop and say so.
3. The root `sdlc.yaml` pins 0.2.24 (line 92; the owner's row in PROGRESS session 8) and the sample's `main` pins 0.2.24 with the 0.2.24 workflow copies. Session 8 could not open the sample's upgrade PR (precondition 1): session 9 opens it first, the way sessions 5–7 did (`git rm .github/workflows/sdlc-*.yml`, `plugin/init/sdlc_init.py --root <sample>`, `git restore -- sdlc.yaml`, one PR; the pin is the owner's edit after the tag), unless the owner did it by hand. Until the sample runs 0.2.24, a Go whose setup step fails is still a red run and a fix round still reads the PR itself.
4. `python tasks.py check` green (1175 tests at the end of session 8) and `claude plugin validate .` clean; the dev tools are not in the container at start (`python -m pip install -e ".[dev]"` first; NOTES §16).
5. Sub-agents named `model: "opus"` explicitly.
6. **The date.** Sixteen days *with* counted runs of the sample's `CI` workflow since 2026-09-25 (choice 92): count them (`actions_list` on `ci.yml`, excluding the runs by `github-actions[bot]`, D12) before planning the live part; the earliest possible day is 2026-10-11. Below sixteen, the scheduled run logs "insufficient baseline" and the session does the rest of this brief.

## Read in this order
1. `docs/PROGRESS.md` — the session-8 record (what was built, choices 95–98, the guardrail rows, the questions, the known gaps), then the session-7 and session-6 records (the live-check table's run URLs are the reference for a healthy run).
2. `docs/NOTES.md` §14, §15 and §16 — the platform facts of the live runs, the environment facts, and the two Actions facts §16 records as *not verified here* (the runner's default shell without `shell:`; `steps.<id>.outcome` under `continue-on-error`): the first live run of the 0.2.24 runbook workflow is their check.
3. `docs/OPERATING_MODEL.md` §4.2 (the `sdlc-fix.yml`, `sdlc-runbook.yml` and `sdlc-detect.yml` rows and the pin paragraph as 0.2.24 changed them) and §9.
4. `docs/ROADMAP.md` and the two proposals under `docs/proposals/` — read, not acted on, unless the owner has accepted one (its roadmap row then carries a change id).
5. `plugin/detect/stats.py`, `plugin/detect/source.py`, `plugin/detect/cli.py` (`cmd_run`, `open_incidents`, `approved_files`, `cmd_park`), `plugin/ci/fix_requests.py`.
6. `docs/MODEL_ALLOCATION.md` §2 and §5c.

## Objective
The objective of session 8, unchanged: read the first real (un-forced) detection run of the sample and record whether the statistics, the diagnosis's use of `gh run view --log-failed` under the sandbox and the failed-run links behave; provoke one real 2σ once the baseline exists; close whatever the run shows, with a test per defect and an eval case per model lesson. Plus the two live checks 0.2.24 owes: a fix round whose `evidence/fix-requests.json` shows what the API returned, and (if a Go's setup step ever fails) the park instead of a red run. Then the owner's decisions.

## Deliverables (in dependency order)
1. **The sample at 0.2.24** (precondition 3): the upgrade PR, opened by the session, merged by the owner; then one dispatched detect run to see the pin step's new line (`git show … | python -`) read the pin and the framework check out at `v0.2.24`.
2. **The first real detection** (only with sixteen points, precondition 6): as session 8's brief said — the scheduled 05:41 UTC run's log and the `sdlc-detect-log-<run id>` artifact, then `detection.json` on the filed branch (mean, sigma, `n_baseline`, `n_tail`, the rule, `forced: false`); if the baseline exists and nothing breached, provoke one bad day the article's way (p.45: one failure after a clean run) with a sample PR whose test fails on purpose, closed without a merge and without a comment; read the diagnosis transcript for the `gh run view --log-failed` calls and record the outcome (allowed, denied, never called) and the fix if denied (the bands' `tools` line or the sandbox's allowed domains).
3. **The fix round's file, live**: one "Request changes" review by the owner on any open sample PR (a rehearsal's intent PR will do) and, after the round, `evidence/fix-requests.json` on the head: the reviews and threads as the API returned them, `not_applied` empty or right, `unavailable` null. A shape mismatch against GitHub's API is a defect with a test (NOTES §16 says which shapes were not re-verified).
4. **Gaps from the runs** (and any fix deliverable 5 produces), each with a test, and an eval case per model lesson, in this session's PR (plugin 0.2.25 if anything in `plugin/` or `template/` changes; the template's copies and this repository's identical; the sample upgraded as in deliverable 1).
5. **The `--bare` check** (carried since session 7): if any phase run of the sample or of another project has run with `ANTHROPIC_API_KEY`, read its `evidence/claude-<phase>.json` for the project's `CLAUDE.md` content in the system prompt; absent → file the fix (pass it explicitly in `run_phase.py`; NOTES §10/§13 say bare mode skips it); present → close the gap. No such run → carry it.
6. **The owner's answers**, if given by then (PROGRESS session 8, "Questions for the owner"): the Milestone 1 bump (a `plugin.json` edit the session makes in the PR; the tag follows the merge); the `accept` verb (a new decision: draft the DECISIONS entry, do not build before it is accepted); the Windows items (build only what the owner confirmed); R1 or R2 accepted → the roadmap row gets the change id and the work goes through `changes/<id>-<slug>/intent.md` with `Entry route: idea`, `Framework change: yes`, in a separate change, not in this session's fix PR.
7. **The carried small items** (only where a fix touches the same code): the `Diff.files` conversion; the per-decision panel cost (choice 52); the release-page backfill for v0.2.15–v0.2.17 (optional); the deploy/status/rollback tools when an adapter names a real deployment (choice 35); a Node flaky-test runbook when a Node project needs one; the phase workflows' setup step reading the head's `commands.setup` (choice 90); the fix round's private-membership case (a member whose organization membership is private reads as `CONTRIBUTOR` and is listed under `not_applied`, choice 97: visible, not solved).
8. **Docs**: `docs/PROGRESS.md` rewritten for session 9 on top (the session-8 record kept below as written); `docs/NOTES.md` §17 with the runs' facts (read date on every fact, choice 93; the two unverified Actions facts of §16 confirmed or corrected); `docs/OPERATING_MODEL.md` only where the code changed; `docs/ROADMAP.md` rows updated if a candidate moved; the next `HANDOFF.md` with this one moved to `docs/handoffs/session-9-first-breach-2.md`.

## What the owner decides next (from sessions 7 and 8)
- The number of the Milestone 1 bump (`docs/ROADMAP.md`, "How it is marked"), once the first real detection is read.
- R1 (`docs/proposals/okf-adoption.md`) and R2 (`docs/proposals/session-tooling-pdf-web.md`): accept, order, or leave proposed. R2's edit tables are the owner's to apply; nothing in them was committed. Session 8 met R2's tooling gap again (the dev tools absent at start, `docs.github.com` unreachable): NOTES §16.
- The two "ask, do not build" items carried since session 5: the `accept` verb for a review finding; session 3's Windows items.
- The session scope: starting the session with both repositories selected is the owner's act (precondition 1).

## Rules for this session
- Never reopen a decision; a decision a live run shows impossible as built is documented in PROGRESS.
- Cite the PDF page whenever the article is used.
- No notifications, no bypass-permissions mode, no third-party runtime dependency, Python for every script, workflows whose `run:` lines call `python` (and the pinned `claude` install) only — the one exception is the pin step's `git show … | python -` line under `shell: bash` (choice 95) — never with a `${{ }}` in the line (an `env:` value may carry one).
- Guardrail files here and in the sample (`CLAUDE.md`, `.claude/**`, `REVIEW.md`, `sdlc.yaml`, `bands.yaml`): the owner's edit, given as "link; row; what is there; what it needs to be".
- One open incident per metric (choice 69): a real finding blocks the metric until its PR is triaged; triage it (merge, `schedule` or close) before expecting the next filing, and remember that a close with a comment dismisses its signature for one window.
- The shakedown method of session 6 (choice 88) when a fix must be live-tested before the tag: a sample branch whose `sdlc-detect.yml` checks out the framework at the branch under test, dispatched with that ref.
- Every PR: `python -m compileall -q plugin tests tasks.py .github/scripts`, the full `python -m pytest`, `python -m ruff check .`, `python -m ruff format --check .` — green before the push, chained on the exit codes; a fresh-context Opus review of the diff before the final commit; a draft PR against `main`.
- The secrets-check hook refuses any file whose text contains a credential-shaped string: describe a key in words.
- An environment fact (what the container can reach or run) is recorded in NOTES with its read date and re-tested only when the date is older than the environment's last change (choice 93).
